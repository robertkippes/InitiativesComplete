@page
@model Initiatives.Pages.MetaTags.EditModel

@{
    ViewData["Title"] = "Edit";
}

<h2>Edit</h2>

<h4>MetaTag</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="MetaTag.MetaTagId" />
            <input type="hidden" asp-for="MetaTag.IsActive" />
            <div class="form-group">
                <label asp-for="MetaTag.MetaTagShortDescription" class="control-label"></label>
                <input asp-for="MetaTag.MetaTagShortDescription" class="form-control" />
                <span asp-validation-for="MetaTag.MetaTagShortDescription" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MetaTag.MetaTagDescription" class="control-label"></label>
                <input asp-for="MetaTag.MetaTagDescription" class="form-control" />
                <span asp-validation-for="MetaTag.MetaTagDescription" class="text-danger"></span>
            </div>
                     
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-default" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-page="./Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
