@page "{id:int}"
@model Initiatives.Pages.Notes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Note[0].InitiativeNavigation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Note[0].Note1)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Note[0].UserName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Note[0].CreateDate)
            </th>

            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Note)
        {
            <tr>
                <td>
                    <a asp-page="/EAInitiatives/Edit" asp-route-id="@item.InitiativeId">@Html.DisplayFor(modelItem => item.InitiativeNavigation.EngagementName)</a>
                </td>
                <td>
                    <a asp-page="./Edit" asp-route-id="@item.NoteId"> @Html.DisplayFor(modelItem => item.Note1)</a>
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.UserName)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreateDate)
                </td>
                <td>
                    <a asp-page="./Delete" asp-route-id="@item.NoteId">Delete</a>
                </td>
            </tr>
        }
    </tbody>
</table>
<a asp-page="/EAInitiatives/Index">Back to List</a>
