@page
@model Initiatives.Pages.SolutionTypes.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.DisplayNameFor(model => model.SolutionType[0].SolutionTypeShortDescription)
                </th>
                <th>
                    @Html.DisplayNameFor(model => model.SolutionType[0].SolutionTypeDescription)
                </th>
                <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.SolutionType) {
        <tr>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SolutionTypeId">@Html.DisplayFor(modelItem => item.SolutionTypeShortDescription)</a></td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.SolutionTypeId">@Html.DisplayFor(modelItem => item.SolutionTypeDescription)</a></td>
            
            <td>
                <a asp-page="./Delete" asp-route-id="@item.SolutionTypeId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
